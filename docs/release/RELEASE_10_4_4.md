# Restaurant OS 10.4.4 — Payment recovery

Gateway:
- Восстановление `payments_json` из исторического `payload_json`.
- Finance автоматически запускает безопасную recovery-проверку.
- Добавлена кнопка «Восстановить историю оплат».
- Legacy snapshots 10.4.2/10.4.3 могут начать показывать оплаты без повторной загрузки SQL.

Agent 31.4.1:
- Подробный лог `Оплаты: дата=..., строк=N`.
- Ошибка `payment_summary` логируется отдельно.
- Ошибка оплат больше не прерывает весь sales snapshot.
- В лог отправки snapshot добавлено количество строк оплат.
