# Restaurant OS 10.2.1

Стабилизационный релиз Drawer индекса.

Исправлено:

- удалён вызов отсутствующей функции `scoreStatus`;
- статус индекса рассчитывается локально и безопасно;
- закрытие Drawer защищено от повторных вызовов;
- бизнес-логика Dashboard не изменялась.
