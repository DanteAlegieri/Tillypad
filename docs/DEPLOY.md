# Deployment

Обычный выпуск выполняется через `scripts/release.ps1`.

Ручной `deploy_vps.ps1` остаётся доступен для диагностики и тестовых сборок.

После успешного деплоя Release Manager проверяет:

- `/health`;
- поле `ok`;
- точное совпадение версии.
