# Tillypad Dashboard v1.3

Первая стабильная архитектурная версия проекта для «Гастродома №3».

## Возможности

- прямое подключение к SQL Server Tillypad через Radmin VPN;
- SQL-дашборд по таблице `dbo.tp_Guests`;
- SQL Explorer:
  - поиск таблиц;
  - количество строк;
  - поиск колонок;
  - структура таблицы;
  - безопасный `SELECT TOP`;
- логирование;
- настройки через `.env`;
- разделение кода на:
  - подключение к БД;
  - репозитории;
  - сервисы;
  - веб-маршруты;
  - шаблоны.

Все SQL-запросы используют только `SELECT`.

## Установка

1. Сохраните старый файл `.env`.
2. Распакуйте архив в:

```text
C:\TillypadDashboard
```

3. Скопируйте `.env.example` в `.env`.
4. Заполните:

```env
TILLYPAD_SQL_SERVER=26.187.75.193
TILLYPAD_SQL_PORT=1433
TILLYPAD_SQL_DATABASE=TillypadSegment
TILLYPAD_SQL_USER=ваш_sql_логин
TILLYPAD_SQL_PASSWORD=ваш_sql_пароль
TILLYPAD_SQL_DRIVER=ODBC Driver 18 for SQL Server
TILLYPAD_SQL_ENCRYPT=no
TILLYPAD_SQL_TRUST_CERTIFICATE=yes
TILLYPAD_SQL_TIMEOUT=10
```

5. Запустите `install.bat`.
6. Запустите `run.bat`.

## Адреса

```text
http://127.0.0.1:8000/
http://127.0.0.1:8000/sql
http://127.0.0.1:8000/sql-explorer
http://127.0.0.1:8000/sql-test
```

## Git

```cmd
git add .
git commit -m "Release stable architecture v1.3"
git push
```


## Карта базы

Страница: `http://127.0.0.1:8000/schema-map`

Показывает таблицы, колонки, первичные и внешние ключи и фильтрацию по ключевым словам.


## Продажи

Новая страница:

```text
http://127.0.0.1:8000/sales
```

Показывает:

- выручку за выбранный период;
- количество чеков;
- средний чек;
- количество проданных единиц;
- продажи по часам;
- топ блюд;
- последние чеки.

Текущая формула:

```text
orit_Count × orit_Price
```

Пока отчёт не исключает отменённые позиции, возвраты и служебные движения.
Следующий этап — определить назначение `orit_mvtp_ID` и статусов заказа.


## v1.3

- современное боковое меню;
- интерактивные графики Chart.js;
- динамика выручки по дням;
- продажи по часам;
- сравнение с предыдущим периодом;
- показатели роста/снижения;
- обновлённый рейтинг блюд;
- адаптивный интерфейс.


## v1.3.1

Исправлена ошибка 500 на `/sales`: методы `daily_sales()` и `comparison()` находятся внутри класса `SalesRepository`.


## v1.4

Добавлен раздел «Меню»:

- каталог блюд;
- фильтр по датам;
- поиск по названию и артикулу;
- фильтр по группе;
- текущая цена;
- активность позиции;
- количество продаж;
- количество чеков;
- выручка по позиции.


## v1.4.1

Исправлена ошибка страницы `/menu`.

Причина: в Jinja выражение `result.items` обращалось к методу словаря `items`,
а не к ключу `"items"`. Шаблон переведён на явное обращение
`result["items"]`.


## v1.5 — Финансы

Добавлено:

- сумма оплат;
- наличные и безналичные;
- средний оплаченный чек;
- разбивка по `tp_PayTypes`;
- доля каждого способа оплаты;
- круговая диаграмма;
- динамика оплат по дням;
- оплаты по часам;
- последние платежи;
- отображение `chpy_chpc_ID`;
- предварительное выявление возвратов по отрицательной сумме.


## v1.6 — Улучшенный блок оплат

Добавлено:

- карточки способов оплаты;
- прогресс-бары доли оплаты;
- сумма, доля и средняя оплата по каждому способу;
- более компактная легенда диаграммы;
- улучшенный адаптивный вид для телефона.


## v1.7 — Операционный центр

Главная страница заменена на рабочий экран владельца:

- оплачено сегодня;
- наличные и безналичные;
- количество и средний чек;
- открытые и закрытые гостевые счета;
- среднее время обслуживания;
- последний чек;
- лидер продаж дня;
- оплаты по часам;
- последние платёжные операции.

CRM клиентов в эту версию не включена: для неё требуется определить
реальную таблицу клиентов, чтобы не путать клиентов с гостевыми счетами.


## v1.8 — Операционный центр 2.0

Добавлено:

- настраиваемый дневной план выручки;
- прогресс до плана;
- остаток до цели;
- сравнение выручки и количества чеков со вчера;
- график денег и чеков по часам;
- доля безналичных оплат в главной карточке;
- единая очистка названий способов оплаты на главной.

В `.env` можно задать:

```env
DAILY_REVENUE_TARGET=10000
```


## v2.0 — Restaurant BI

Добавлен новый раздел `/bi`:

- ABC-анализ по выручке;
- XYZ-анализ стабильности спроса;
- матрица ABC/XYZ;
- корзина покупателей;
- выручка по дням недели;
- автоматические инсайты;
- подробная таблица классификации меню.

XYZ рассчитывается по коэффициенту вариации ежедневных продаж:

- X: до 10%;
- Y: от 10% до 25%;
- Z: выше 25%.
