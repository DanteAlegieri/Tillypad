# Restaurant OS 9.1.0

Исправлены отсутствующие функции:

- `activatePeriodButton()`;
- `setPeriodInUrl()`.

Управление кнопками периодов и URL вынесено в отдельный ES-модуль `period-controls.js`. Основной рабочий стол запускается через `dashboard/init.js`.
